```bash
ls <dir>
```
view files and folder inside `<dir>`

```bash
cp <file> <destination>
```
#### Note
> you can change the name of the file when you copy it
##### For example
```bash
cp cat.txt /opt/colors/white.txt
```
